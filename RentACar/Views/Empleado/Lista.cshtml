@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<RentACar.Models.Empleado>;

<div class="row">
    <div class="">
        <h2>Empleados</h2>
        <a asp-controller="Empleado" asp-action="Nuevo" class="btn btn-primary mb-3">Nuevo</a>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Cédula</th>
                    <th>Tanda de labor</th>
                    <th>Comisión</th>
                    <th>Fecha de contratación</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nombre</td>
                        <td>@item.Cedula</td>
                        <td>@item.TandaLabor</td>
                        <td>@item.PorcientoComision</td>
                        <td>@item.FechaContratacion</td>
                        <td>@item.Estado</td>
                        <td>
                            <a asp-controller="Empleado" asp-action="Editar" asp-route-Id="@item.Id" class="btn btn-success">Editar</a>
                            <a asp-controller="Empleado" asp-action="Eliminar" asp-route-Id="@item.Id" onclick="return confirmar()" class="btn btn-danger">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        function confirmar() {
            return confirm("¿Estás seguro de eliminar este empleado?")
        }
    </script>
}